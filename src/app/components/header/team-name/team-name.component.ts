import { Component, OnInit } from '@angular/core';
import { Document as TeamsDocument } from '../../../services/api/teams/document';
import { Team } from '../../../services/api/teams/model';

@Component({
  selector: 'app-team-name',
  templateUrl: './team-name.component.html',
  styleUrls: ['./team-name.component.scss']
})
export class TeamNameComponent {

  // team: TeamsDocument;

  // constructor(teamDocument: TeamsDocument) {
  //   this.team = teamDocument;
  // }

}
